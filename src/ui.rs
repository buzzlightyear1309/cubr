use bevy::prelude::*;

/// Native bevy_ui panel of 18 move buttons -> MoveQueue. Phase 5 implements it.
pub struct UiPlugin;

impl Plugin for UiPlugin {
    fn build(&self, _app: &mut App) {
        // Phase 5 builds the move-button panel and the press -> MoveQueue system.
    }
}
